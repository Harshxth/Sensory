"use client";

// F1.8 — pulsing icons on map + banner notification when zoomed near.
import type { Alert } from "@/types";

export function AlertPulse(_props: { alerts: Alert[] }) {
  return null;
}
