"use client";

// F1.6 — toggleable wheelchair layer. Recolors map: blue accent / gray / striped.
// TODO: query /api/wheelchair on bbox change, render symbol layer.

export function WheelchairLayer(_props: { enabled: boolean }) {
  return null;
}
