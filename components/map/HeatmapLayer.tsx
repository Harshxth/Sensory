"use client";

// F1.2 - colored dots driven by sensory.composite. Pulse animation on dots with recent reviews.
// TODO: subscribe to /api/venues, render Mapbox circle layer with paint.circle-color step expression.

import type { Venue } from "@/types";

export function HeatmapLayer(_props: { venues: Venue[] }) {
  return null;
}
